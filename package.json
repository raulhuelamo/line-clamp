{
  "name": "line-clamp",
  "version": "0.0.4",
  "description": "Truncate multi-line text in a DOM element.",
  "author": "Lim Yuan Qing",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/yuanqing/line-clamp.git"
  },
  "devDependencies": {
    "concurrently": "^3.5.1",
    "ecstatic": "^3.2.0",
    "gzip-size-cli": "^2.1.0",
    "opn-cli": "^3.1.0",
    "prettier-standard": "^8.0.0",
    "uglify-js": "^3.3.11",
    "watchify": "^3.10.0"
  },
  "scripts": {
    "start": "concurrently \"watchify index.js --standalone lineClamp --outfile bundle.js\" \"ecstatic --port 8080\" \"opn 'http://0.0.0.0:8080/example/'\"",
    "lint": "prettier-standard index.js --no-semi --single-quote --write",
    "test": "echo \"Error: no test specified\" && exit 1",
    "weight": "uglifyjs index.js --compress --mangle --toplevel | gzip-size"
  },
  "keywords": [
    "clamp",
    "dom",
    "ellipsis",
    "html",
    "line clamp",
    "line-clamp",
    "text",
    "truncate",
    "truncation"
  ]
}
